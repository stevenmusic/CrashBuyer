name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  # A push made with GITHUB_TOKEN does not trigger `push`, so the daily data
  # commit is picked up by chaining off that workflow's completion instead.
  workflow_run:
    workflows: ['Update S&P 500 data']
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    # Nothing to publish if the data refresh failed.
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - id: deployment
        uses: actions/deploy-pages@v4
